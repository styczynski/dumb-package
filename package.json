{
  "name": "dumb-package",
  "version": "0.1.0",
  "devDependencies": {
    "jquery": "*"
  },
  "dependencies": {
    "lodash": "*"
  },
  "scripts": {
    "install": "[ -f ThisIsNpmPackage ] && npm install . && node ./build.js"
  }
}

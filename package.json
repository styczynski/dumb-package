{
  "name": "dumb-pacakge",
  "version": "0.1.1",
  "devDependencies": {
    "jquery": "*"
  },
  "dependencies": {
    "lodash": "*"
  },
  "scripts": {
    "install": "[ -f ThisIsNpmPackage ] && npm install . && node ./build.js"
  }
}
